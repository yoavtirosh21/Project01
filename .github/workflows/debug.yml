name: Debug Script

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
      - uses: actions/checkout@v2
      - name: Install Keyboard
      - run: pip install keyboard
      - name: Test Code
      - run: py test.py
